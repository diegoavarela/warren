{
  "buildCommand": "cd ../warren && npm install && cd ../shared && npm install && cd ../admin-portal && npm install --legacy-peer-deps && NODE_PATH=../warren/node_modules npm run build",
  "installCommand": "npm install --legacy-peer-deps"
}