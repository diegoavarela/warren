{
  "buildCommand": "cd ../warren && npm install && cd ../shared && npm install && cd ../admin-portal && npm install && NODE_PATH=../warren/node_modules npm run build",
  "installCommand": "npm install"
}