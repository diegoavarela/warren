{
  "buildCommand": "cd ../warren && npm install && cd ../shared && npm install && cd ../admin-portal && npm install --legacy-peer-deps && npm run build:standalone",
  "installCommand": "npm install --legacy-peer-deps"
}